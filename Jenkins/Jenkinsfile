@Library('BranchingPOC') _
pipeline {
   agent any
   parameters {
    stashedFile 'SOD-Tickets'
  }
   
   stages {
        stage('Example') {
      steps {
      
        unstash 'SOD-Tickets'
         script{
            def data = sh (script: "cat SOD-Tickets", returnStdout: true) 
        echo 'this is data'
        println data
         }
        
      }
    }
       
   }
  }
