node {
properties([
    pipelineTriggers([
      pullRequestReview(reviewStates: ['approved'])
    ])
])
    
        
    stage('Deploy') {
        
            ullRequest.merge(commitTitle: 'Make it so..', commitMessage: 'TO BOLDLY GO WHERE NO MAN HAS GONE BEFORE...', mergeMethod: 'squash')
        
    }
      
    
}
